import React from 'react';
import { Layout, PageHeader, Button } from 'antd';
import { CloseOutlined } from '@ant-design/icons';

export const Header = () => {
  return (
    <PageHeader
      title="Shibob"
      subTitle="For your raffle botting needs."
      extra={[

      ]}
    />
  )
}